export class Cookie {
  cookieSize: number;
  cookieShape: string;
  cookieType: string;

  constructor(cookieSize: number, cookieShape: string, cookieType: string) {
    this.cookieSize = cookieSize;
    this.cookieShape = cookieShape;
    this.cookieType = cookieType;
  }
}
